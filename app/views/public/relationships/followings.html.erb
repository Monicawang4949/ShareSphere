<div class="container-fluid text-center" style="color: white">
  <div class="row">

    <div class="col-md-2 offset-md-1">
      <%= render 'public/users/sidebar' %>
    </div>

    <div class="col-md-8">
      <div class="row">
        <h2 class="col-md-5 p-4"><%= link_to @user.name, user_path(@user), style: "text-decoration: none; color: #0dcaf0" %> さんのフォロー</h2>
      </div>
      <% if @users.exists? %>
        <%= render "public/users/index", users: @users %>
      <% else %>
        <p>ユーザーはいません</p>
      <% end %>
    </div>

  </div>
</div>