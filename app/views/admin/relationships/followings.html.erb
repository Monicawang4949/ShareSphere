<div class="container-fluid text-center" style="color: white">
  <div class="row">

    <div class="col-md-2 offset-md-1">
      <%= render 'admin/users/sidebar' %>
    </div>

    <div class="col-md-8">
      <h2><%= link_to @user.name, admin_user_path(@user), style: "text-decoration: none; color: #0dcaf0" %> さんのフォロー</h2>
      <% if @users.exists? %>
        <%= render "admin/users/index", users: @users %>
      <% else %>
        <p>ユーザーはいません</p>
      <% end %>
    </div>

  </div>
</div>